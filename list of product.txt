CREATE DATABASE system;



CREATE TABLE products (
   
 id INT AUTO_INCREMENT PRIMARY KEY,
   
 name VARCHAR(255),
    
price DECIMAL(10, 2),
    
quality VARCHAR(50)
);


INSERT INTO products 
(name, price, quality)
 VALUES

('Product A', 10.00, 'high'),

('Product B', 20.00, 'medium'),

('Product C', 15.00, 'low');

SELECT SUM(price) AS total_value FROM products;